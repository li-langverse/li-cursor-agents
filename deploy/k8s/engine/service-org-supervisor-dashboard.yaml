apiVersion: v1
kind: Service
metadata:
  name: li-org-supervisor-dashboard
  namespace: li-swarm
  labels:
    app: li-org-supervisor-dashboard
spec:
  type: NodePort
  selector:
    app: li-org-supervisor-dashboard
  ports:
    - name: http
      port: 9478
      targetPort: http
      nodePort: 30478