apiVersion: apps/v1
kind: Deployment
metadata:
  name: li-ph-sci-electrochemistry
  namespace: li-swarm
  labels:
    app: li-ph-sci-electrochemistry
    app.kubernetes.io/name: li-ph-sci-electrochemistry
    app.kubernetes.io/component: goal-directed-agent
    li-langverse.io/sprint: ph-sci-electrochemistry
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: li-ph-sci-electrochemistry
  template:
    metadata:
      labels:
        app: li-ph-sci-electrochemistry
        app.kubernetes.io/name: li-ph-sci-electrochemistry
        app.kubernetes.io/component: goal-directed-agent
        li-langverse.io/sprint: ph-sci-electrochemistry
    spec:
      nodeSelector:
        kubernetes.io/hostname: engine
      imagePullSecrets:
        - name: ghcr-li-langverse
      containers:
        - name: ph-sci-electrochemistry
          image: ghcr.io/li-langverse/li-cursor-agents:proof-explorer-llvm22
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "/scripts/entrypoint.sh"]
          envFrom:
            - configMapRef:
                name: li-ph-sci-electrochemistry
          env:
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: CURSOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_API_KEY
                  optional: true
            - name: CURSOR_SDK_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_SDK_KEY
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "4"
              memory: "4Gi"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: entrypoint
              mountPath: /scripts
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: li-ph-sci-electrochemistry-workspace
        - name: entrypoint
          configMap:
            name: li-ph-sci-electrochemistry-entrypoint
            defaultMode: 0755
