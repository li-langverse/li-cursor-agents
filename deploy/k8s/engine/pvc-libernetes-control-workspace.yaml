apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: li-libernetes-control-workspace
  namespace: li-swarm
  labels:
    app: li-libernetes-control
    app.kubernetes.io/component: goal-directed-agent
    li-langverse.io/sprint: libernetes-control
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-path