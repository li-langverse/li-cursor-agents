apiVersion: apps/v1
kind: Deployment
metadata:
  name: li-world-studio-gui-product-visual
  namespace: li-swarm
  labels:
    app: li-world-studio-gui-product-visual
    app.kubernetes.io/name: li-world-studio-gui-product-visual
    app.kubernetes.io/component: goal-directed-agent
    li-langverse.io/sprint: world-studio-gui-product-visual
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: li-world-studio-gui-product-visual
  template:
    metadata:
      labels:
        app: li-world-studio-gui-product-visual
        app.kubernetes.io/name: li-world-studio-gui-product-visual
        app.kubernetes.io/component: goal-directed-agent
        li-langverse.io/sprint: world-studio-gui-product-visual
    spec:
      nodeSelector:
        kubernetes.io/hostname: engine
      imagePullSecrets:
        - name: ghcr-li-langverse
      containers:
        - name: world-studio-gui-product-visual
          image: ghcr.io/li-langverse/li-cursor-agents:proof-explorer-llvm22
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "/scripts/entrypoint.sh"]
          envFrom:
            - configMapRef:
                name: li-world-studio-gui-product-visual
          env:
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: CURSOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_API_KEY
                  optional: true
            - name: CURSOR_SDK_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_SDK_KEY
                  optional: true
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "8"
              memory: "8Gi"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: entrypoint
              mountPath: /scripts
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: li-world-studio-gui-product-visual-workspace
        - name: entrypoint
          configMap:
            name: li-world-studio-gui-product-visual-entrypoint
            defaultMode: 0755

