apiVersion: apps/v1
kind: Deployment
metadata:
  name: li-libernetes-platform
  namespace: li-swarm
  labels:
    app: li-libernetes-platform
    app.kubernetes.io/component: goal-directed-agent
    li-langverse.io/sprint: libernetes-platform
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: li-libernetes-platform
  template:
    metadata:
      labels:
        app: li-libernetes-platform
        app.kubernetes.io/component: goal-directed-agent
        li-langverse.io/sprint: libernetes-platform
    spec:
      nodeSelector:
        kubernetes.io/hostname: engine
      imagePullSecrets:
        - name: ghcr-li-langverse
      containers:
        - name: libernetes-platform
          image: ghcr.io/li-langverse/li-cursor-agents:proof-explorer-llvm22
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "/config/entrypoint.sh"]
          envFrom:
            - configMapRef:
                name: li-libernetes-platform
            - configMapRef:
                name: li-goal-worker-runtime
          env:
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
                  optional: true
            - name: GITLAB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GITLAB_TOKEN
                  optional: true
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: CURSOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_API_KEY
                  optional: true
            - name: CURSOR_SDK_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_SDK_KEY
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "2Gi"
            limits:
              cpu: "4"
              memory: "8Gi"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: git-bundle
              mountPath: /config
            - name: git-auth
              mountPath: /git-auth
              readOnly: true
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: li-libernetes-platform-workspace
        - name: git-bundle
          configMap:
            name: li-libernetes-git-bundle
            defaultMode: 0755
        - name: git-auth
          configMap:
            name: li-k8s-git-auth
            defaultMode: 0755