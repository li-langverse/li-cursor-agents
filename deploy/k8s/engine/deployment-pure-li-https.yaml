apiVersion: apps/v1
kind: Deployment
metadata:
  name: li-pure-li-https
  namespace: li-swarm
  labels:
    app: li-pure-li-https
    app.kubernetes.io/name: li-pure-li-https
    app.kubernetes.io/component: goal-directed-agent
    li-langverse.io/sprint: pure-li-https
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: li-pure-li-https
  template:
    metadata:
      labels:
        app: li-pure-li-https
        app.kubernetes.io/name: li-pure-li-https
        app.kubernetes.io/component: goal-directed-agent
        li-langverse.io/sprint: pure-li-https
    spec:
      nodeSelector:
        kubernetes.io/hostname: engine
      imagePullSecrets:
        - name: ghcr-li-langverse
      containers:
        - name: pure-li-https
          image: ghcr.io/li-langverse/li-cursor-agents:proof-explorer
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "/scripts/entrypoint.sh"]
          envFrom:
            - configMapRef:
                name: li-pure-li-https
          env:
            - name: GH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: GH_TOKEN
            - name: CURSOR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_API_KEY
                  optional: true
            - name: CURSOR_SDK_KEY
              valueFrom:
                secretKeyRef:
                  name: li-agents-secrets
                  key: CURSOR_SDK_KEY
                  optional: true
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "4"
              memory: "4Gi"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            - name: entrypoint
              mountPath: /scripts
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: li-proof-explorer-workspace
        - name: entrypoint
          configMap:
            name: li-pure-li-https-entrypoint
            defaultMode: 0755