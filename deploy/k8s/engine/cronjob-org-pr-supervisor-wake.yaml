apiVersion: batch/v1
kind: CronJob
metadata:
  name: li-org-pr-supervisor-wake
  namespace: li-swarm
  labels:
    app: li-org-pr-supervisor-wake
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 3600
      backoffLimit: 1
      activeDeadlineSeconds: 300
      template:
        metadata:
          labels:
            app: li-org-pr-supervisor-wake
        spec:
          serviceAccountName: li-org-pr-supervisor
          restartPolicy: OnFailure
          nodeSelector:
            li-langverse.io/node-pool: engine
          containers:
            - name: wake
              image: ghcr.io/li-langverse/li-cursor-agents:latest
              imagePullPolicy: Always
              command: ["node", "dist/cli/org-pr-supervisor.js", "wake"]
              envFrom:
                - configMapRef:
                    name: li-org-pr-supervisor
              resources:
                requests:
                  cpu: "50m"
                  memory: "128Mi"
                limits:
                  cpu: "500m"
                  memory: "256Mi"
