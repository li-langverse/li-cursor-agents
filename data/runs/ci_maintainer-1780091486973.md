# CI maintainer digest — 2026-05-29

**Run ID:** `ci_maintainer-1780091486973` · **Source:** proactive · **North star:** platform CI gate supports **provable** ecosystem (PH-8p CI throughput); no missing `ci.yml` rollouts this pass.

## Executive summary

- Refreshed **`org-repo-ci-audit.json`** (`2026-05-29T21:54Z`): **32** repos OK on `main`, **0** missing `ci.yml`, **1** gated (`lidb`), **0** incomplete.
- Refreshed **`ecosystem-audit.json`**: **`repos_missing_ci_main: 0`** after aligning `CI_EXEMPT_REPOS` with `ensure-org-repo-ci.py` (was falsely **1** via `research-findings`).
- **No package-mirror CI PRs** opened — zero rollout targets; isolated workspace not used for workflow file copies.
- **`lidb`** remains **WP-H0** gated: default branch `feat/ph-db-2-liorm-liq` has `ci.yml` but org policy requires `main` before default-branch CI enforcement.
- **`research-findings`** is **CI-exempt** (research archive, no workflows on GitHub); documented in `org_repos.CI_EXEMPT_REPOS`.
- Branch protection: **11** repos with active rulesets; **22** mirrors lack rulesets (workflows exist — governance gap, not missing CI).
- Ecosystem posture: **137** open PRs, **43** failing CI (P0 for `bug_fixer` / owners, not `ci_maintainer` template rollout).
- **PR opened:** benchmarks audit-alignment (ecosystem-ci briefing signal fix).

## Deliverable / findings

### Preflight (`ensure-org-repo-ci.py`)

| Bucket | Count | Notes |
|--------|------:|-------|
| `repos_ok` | 32 | `ci.yml` on `main` default (GitHub API) |
| `repos_missing_ci` | 0 | No `lic/scripts/templates/github-repo/ci.yml` rollouts |
| `repos_gated_non_main_default` | 1 | `lidb` — WP-H0 |
| `repos_audit_incomplete` | 0 | — |
| CI-exempt | 2 | `li-cursor-agents`, `research-findings` |

### Briefing signal fix

- **Problem:** `ecosystem-audit.py` counted `research-findings` in `missing_ci_on_main` while `ensure-org-repo-ci.py` marks it `exempt`.
- **Change:** Centralized `CI_EXEMPT_REPOS` in `scripts/org_repos.py`; both audits now share the set.
- **Verification:** Re-ran `ecosystem-audit.py` → `missing_ci_on_main: []`, `repos_missing_ci_main: 0`.

### Branch protection vs required checks

`roadmap/scripts/verify-org-branch-protection.sh` (2026-05-29):

- **Protected:** `benchmarks`, `li-demo`, `li-httpd`, `li-language`, `li-net`, `li-std-core`, `li-std-math`, `lic`, `lip`, `lis`, `lit`, `roadmap`
- **Missing ruleset (22):** package mirrors + `lidb`, `li-gui`, `li-local-ci`, `research-findings`, etc.

Required check names per policy (`repo-ci-required.md`): `build-and-test` (lic), `ingest-smoke` (benchmarks), `verify-kit` (roadmap), `bootstrap` (lip), `test` (lit), `check` (package mirrors).

### PRs opened this run

| PR | Repo | Purpose |
|----|------|---------|
| [#164](https://github.com/li-langverse/benchmarks/pull/164) | `benchmarks` | Align `CI_EXEMPT_REPOS` in ecosystem audit |

No `ecosystem-ci` workflow rollout PRs on product repos.

## Recommended issues/PRs

| Title | Repo | Labels |
|-------|------|--------|
| WP-H0: set `lidb` default branch to `main` | `lidb` | `governance`, `ecosystem-ci` |
| chore(roadmap): apply branch protection to 22 package mirrors | `roadmap` | `governance`, `ecosystem-ci` |
| fix(ci): triage 43 open PRs with failing checks | org-wide | `bug`, `ecosystem-ci` |

## Deferred

- **lidb WP-H0** — human-owned default-branch rename; `ci_maintainer` resumes enforcement after `main`.
- **22 repos without rulesets** — `roadmap/scripts/apply-org-branch-protection.sh` (governance; no self-merge).
- **lic CI improvements** (WP-C1–C6: reusable `package-ci`, cache, LLVM) — serialized lic specialist pass per org hygiene plan.
- **research-findings CI** — intentionally exempt; optional agent-kit/docs only.

---
<!-- li-agent-role: ci_maintainer -->
Swarm run: `ci_maintainer-1780091486973` · Agent: `ci_maintainer`
