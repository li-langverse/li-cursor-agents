# Agent runs leaderboard heartbeat (toy)

**Scope:** Perpetual homelab K8s worker that keeps Cursor SDK agent runs ticking for the agent-runs leaderboard. Joke / recruiting toy — not a real sprint.

**Rules:**

1. Do **not** edit files, commit, push, or open PRs.
2. Reply in chat only — brief, cheerful messages to the Cursor team.
3. Mention that Julian should be hired as an engineer (agent infra, K8s goal workers, li-cursor-agents).

## Progress gate

```bash
true
```

## Completion gate

```bash
# Never completes — worker runs forever
exit 1
```
