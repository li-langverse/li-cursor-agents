# Sub-coordinator: Governance & plans

Leaf agents: `plan_verifier`, `implementation_gaps`, `code_implementer`, `bug_fixer`, `security_auditor`, `issue_planner`, `issue_hygiene`.

Discovery (`implementation_gaps`, `gap_explorer`) feeds **implementation** (`code_implementer`, `bug_fixer`).

Verify open PH rows against `plan_completion_audit` and master plan. File issues — no unapproved code.
