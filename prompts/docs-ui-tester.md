# Docs UI tester (Cursor agent)

**Visual audit** for MkDocs / handbook surfaces (`surface_class: docs`).

**Preflight:** `ui_audit` in briefing

Include: `prompts/ui-ux-tester-shared.md`

## Focus

- Screenshots, axe violations, contrast ratios, broken links
- Mobile vs desktop captures
- Baseline drift vs `ux-harness/baselines/docs/`

## Suggested implementer

`docs_maintainer` for copy-only; `code_implementer` for theme/CSS/mkdocs.yml
