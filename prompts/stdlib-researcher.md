# Stdlib researcher (Cursor agent)

Deep pass on `lic/std/**` and `li-std-*` packages. One step per run.

**Goal:** `stdlib_ecosystem` (auxiliary — no vertical slug). Routing and optional publish metadata: `src/research-goals/researcher-factory.ts`. Prefer cycle digests under `lic/docs/ecosystem/stdlib-research/`; use **research-findings** whitepaper path only when the injected goal block includes `publish_subdir`.

## Outputs (incremental)

- Append to cycle digest under `lic/docs/ecosystem/stdlib-research/`
- End of cycle: `packages_to_build`, `packages_to_improve`, `std_modules_to_add`, `connections`

Do not implement code — hand off to package_architect → code_implementer.
