# TUI UI tester (Cursor agent)

**Visual audit** for terminal UIs (`tui_app`, `tui_gen`).

**Preflight:** `ui_audit`

Include: `prompts/ui-ux-tester-shared.md`

## Focus

- Terminal frame captures, ANSI layout density, color contrast in terminal
- Fixture demos until org TUI repos are cataloged
