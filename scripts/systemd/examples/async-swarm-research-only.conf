# Research-only profile — minimal SDK use; maintenance still on by default.
# Copy to: ~/.config/systemd/user/li-agents-async-swarm.service.d/profile.conf
[Service]
Environment=LI_SWARM_PAUSE_WORKERS=1
Environment=LI_IMPLEMENT_LANE_ENABLED=0
Environment=LI_OBSERVER_DISABLE=1
