# Server / deploy platform — research scaffold

## North star

httpd, service mesh, observability, and secure deploy story vs `web_platform` (WASM/browser). Focus ops/runtime.

## v1 scope

- One deploy journey documented with commands and threat notes.
- Cross-link `web_platform` for frontend/WASM; this goal owns process/supervisor/systemd.
