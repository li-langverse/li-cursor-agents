# Robotics — research scaffold

## North star

Motion planning, control, and perception stacks (ROS2 recipes, Drake, MuJoCo) mapped to Li real-time + sim packages.

## v1 scope

- One vertical slice: kinematics smoke or planner benchmark stub.
- Flag where a **dedicated robotics researcher agent** would help (hardware I/O, ROS bridges).

## Handoff

`code_implementer` only when `allow_implementation` and gates exist.
