# Biology / bioinformatics — research scaffold

## North star

Survey pipelines (alignment, structure, omics) and identify Li packages or std gaps vs Biopython / BioJulia patterns.

## v1 scope

- One falsifiable hypothesis (e.g. parser smoke, small benchmark).
- Whitepaper in `research-findings` with `validity_grade: study-only` until li-tests exist.

## Handoff

`package_architect` when a new `li-bio-*` package is justified.
