# UI/UX quality — v1 scaffold

## North star

Internal docs, GUI, and TUI surfaces meet SOTA UX bar; testers file remediation, implementers ship fixes.

## v1 scope

- Run `ux-harness` preflight; six testers (docs/gui/tui × ui/ux) file issues + queue items
- No product code in tester runs

## Evidence

- `remediation_manifest.json` + GitHub issues with `ready-for-implement` label
- `ui-audit.json` / `ux-audit.json` green after fix
