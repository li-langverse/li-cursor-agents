# Database platform — research scaffold

## North star

Embedded/query-engine SOTA vs SQLite/DuckDB patterns; Li persistence layer (`lidb`, catalog, agent state).

## v1 scope

- Survey + one reproducible query or schema migration note.
- Issues for `package_architect` when new official PKG is needed.
