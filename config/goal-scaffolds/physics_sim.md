# Computational physics — research scaffold

## North star

Map PDE/FEM/continuum incumbents (FEniCS, deal.II, OpenFOAM recipes) to Li numerics + `li-physics-*` packages with **validity locked** before perf claims.

## v1 scope (research)

- One vertical slice: heat equation, linear elasticity, or incompressible flow stub with reference parity.
- Link bench row or `docs/numerics/studies/` size-scaling table.
- Whitepaper under `research-findings/whitepapers/YYYY-MM/physics_sim/<slug>/`.

## Out of scope

- Full OpenFOAM binary oracle until driver exists.
- Trading stability for speed.

## Evidence required

- Whitepaper + optional handoff to `code_implementer` with registry id.
